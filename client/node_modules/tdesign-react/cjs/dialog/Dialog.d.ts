import React from 'react';
import { TdDialogProps, DialogInstance } from './type';
import { StyledProps } from '../common';
export interface DialogProps extends TdDialogProps, StyledProps {
    /**
     * 是否以插件形式调用
     */
    isPlugin?: boolean;
}
declare const _default: React.ForwardRefExoticComponent<DialogProps & React.RefAttributes<DialogInstance>>;
export default _default;
