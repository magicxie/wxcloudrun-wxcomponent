import { DialogMethod, DialogConfirmMethod, DialogAlertMethod } from './type';
export interface DialogPlugin extends DialogMethod {
    alert?: DialogAlertMethod;
    confirm?: DialogConfirmMethod;
}
export declare const dialog: DialogPlugin;
export declare const DialogPlugin: DialogPlugin;
