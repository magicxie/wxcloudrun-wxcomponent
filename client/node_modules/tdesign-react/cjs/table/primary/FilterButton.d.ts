import { TElement } from '../../common';
import { FilterValue, PrimaryTableCol } from '../type';
interface Props {
    columns?: Array<PrimaryTableCol>;
    onChange?: Function;
    innerfiltVal?: FilterValue;
    filterIcon?: TElement;
}
declare function FilterButton(props: Props): PrimaryTableCol<import("../type").TableRowData>[];
export default FilterButton;
