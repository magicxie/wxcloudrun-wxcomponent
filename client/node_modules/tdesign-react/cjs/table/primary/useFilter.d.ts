import { DataType, PrimaryTableCol } from '../type';
import { PrimaryTableProps } from '..';
/**
 * 筛选hook
 * 1.返回加入筛选icon后的title
 * 2.返回筛选后的data
 */
declare function useFilter(props: PrimaryTableProps): [PrimaryTableCol[], DataType[]];
export default useFilter;
