import { FC } from 'react';
import { SortInfo, PrimaryTableCol, SortType } from '../type';
export declare const SortTypeEnum: {
    [key in SortType]: SortType;
};
interface SorterButtonProps {
    column: PrimaryTableCol;
    onChange: Function;
    singleSort: SortInfo;
}
declare const SorterButton: FC<SorterButtonProps>;
export default SorterButton;
