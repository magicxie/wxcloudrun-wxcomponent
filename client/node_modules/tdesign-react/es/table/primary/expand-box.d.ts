import { FC } from 'react';
import { TNode } from '../../common';
interface Props {
    expanded?: boolean;
    handleExpandChange?: Function;
    rowKeyValue?: any;
    row?: Record<string, any>;
    expandIcon?: TNode;
    expandOnRowClick?: boolean;
}
declare const ExpandButton: FC<Props>;
export default ExpandButton;
