import React from 'react';
import { StyledProps } from '../../common';
import { SelectValue, TdOptionProps, TdSelectProps, SelectKeysType } from '../type';
/**
 * Option 组件属性
 */
export interface SelectOptionProps extends StyledProps, TdOptionProps, Pick<TdSelectProps, 'size' | 'multiple' | 'max'> {
    selectedValue?: SelectValue;
    children?: React.ReactNode;
    onSelect?: (value: string | number, context: {
        label?: string;
        selected?: boolean;
        event: React.MouseEvent;
        restData?: Record<string, any>;
    }) => void;
    restData?: Record<string, any>;
    keys?: SelectKeysType;
}
declare const Option: (props: SelectOptionProps) => JSX.Element;
export default Option;
