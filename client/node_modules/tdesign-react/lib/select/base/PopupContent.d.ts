import React from 'react';
import { TdSelectProps, SelectValue } from '../type';
interface SelectPopupProps extends Pick<TdSelectProps, 'value' | 'size' | 'multiple' | 'empty' | 'options' | 'max' | 'loadingText' | 'loading' | 'valueType' | 'keys'> {
    onChange?: (value: SelectValue, context?: {
        label?: string | number;
        restData?: Record<string, any>;
    }) => void;
    /**
     * 是否展示popup
     */
    showPopup: boolean;
    /**
     * 控制popup展示的函数
     */
    setShowPopup: (show: boolean) => void;
    children?: React.ReactNode;
}
declare const PopupContent: (props: SelectPopupProps) => JSX.Element;
export default PopupContent;
